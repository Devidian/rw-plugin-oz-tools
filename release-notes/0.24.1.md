## Connector recovery and Windows update safety

- OZ Tools resets and reprovisions its private Manager connector credential after an authenticated backend session rejects a protected native route with 401.
- Installed plugin updates are safely blocked on Windows servers; installing new plugins, checking updates, and reading release notes remain available.
- The package ships JSON settings and translations while preserving existing-server migration and backups.
