# OZ Tools 0.24.6

- Plugin update checks now respect GitHub's rate limit and keep the last known release notes visible while GitHub requests are temporarily paused.
- Server administrators can optionally configure a GitHub token in the local JSON settings file for authenticated update checks. The token is not shown in the game UI.
