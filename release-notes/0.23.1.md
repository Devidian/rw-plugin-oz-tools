## Batch update usability

- Administrators can now install all pending plugin updates sequentially and reload once after completion.
- The combined release-notes dialog scrolls vertically and matches the individual update action styling.
- Shared tables no longer expose accidental horizontal scrollbars.
