## Catalogue initialization fallback

- Fixed a plugin reload failure introduced with the trusted plugin catalogue.
- Catalogue resource errors no longer prevent OZ Tools from starting.
- The bundled catalogue is resolved through the active Rising World plugin class loader.
- Remote catalogue checks remain available if the bundled fallback cannot be read.
