## Native web security and Manager connector

- Adds authenticated game-managed web routes for compatible OZ plugins.
- Adds the secure Manager WebSocket connector with player-status support.
- Server settings and translations now use the JSON runtime format; existing server data remains compatible.
