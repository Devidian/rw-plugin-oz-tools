# External plugin overlay and Windows update attempts

- feat: list installed external plugins and their versions in the Tools settings overlay, with localized update guidance and GitHub release-note support.
- fix: refresh packaged `*.default.json` files during safe plugin updates while retaining local configuration and data.
- feat: add the default-disabled `allowWindowsUpdate` setting for Windows in-place plugin update attempts with isolated backup and rollback.
